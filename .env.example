# ─────────────────────────────────────────────────────────────────────────────
# AURA — Environment Variables Example
# Copy this file to .env.local and fill in your real values.
# NEVER commit .env.local to version control.
# ─────────────────────────────────────────────────────────────────────────────

# ── Supabase (EU Region — Frankfurt or Ireland) ───────────────────────────────
# These two are safe to expose to the browser (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key — SERVER-SIDE ONLY. Never prefix with NEXT_PUBLIC_
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ── Anthropic Claude API ──────────────────────────────────────────────────────
# SERVER-SIDE ONLY. Never expose this to the browser.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# ── Neonomics (Open Banking — PSD2) ──────────────────────────────────────────
# SERVER-SIDE ONLY. Start with sandbox, switch to live when ready.
NEONOMICS_CLIENT_ID=your-client-id-here
NEONOMICS_CLIENT_SECRET=your-client-secret-here
NEONOMICS_BASE_URL=https://sandbox.neonomics.io
NEONOMICS_AUTH_URL=https://sandbox.neonomics.io/auth/realms/sandbox/protocol/openid-connect/token

# ── App ───────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ── Email (for Notifications) ───────────────────────────────────────────────
RESEND_API_KEY=re_your_provided_key_here
EMAIL_FROM=Aura <do-not-reply@aura.app>
CRON_SECRET=your-cron-secret-here
